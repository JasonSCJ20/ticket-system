# Frontend environment variables
# Copy this file to .env and adjust values as needed.

# Override the API base URL if you need to point to a remote backend.
# If this is not set, the Vite dev server proxy forwards /api to localhost:8001 automatically.
# In production builds, set this to your deployed backend URL.
# VITE_API_URL=http://your-backend-host:8001/api
