apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticket-system.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ticket-system.selectorLabels" . | nindent 4 }}